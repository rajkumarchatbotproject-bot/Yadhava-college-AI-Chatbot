<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Yadav College Chatbot Interface</title>
    <style>
        /* General Styling and Background */
        body {
            font-family: 'Arial', sans-serif;
            background-color: #f0f2f5; /* Light background for the preview */
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            margin: 0;
            padding: 20px;
        }

        /* Chatbot Container */
        .chatbot-container {
            width: 380px;
            height: 550px;
            background-color: #ffffff;
            border-radius: 12px;
            box-shadow: 0 8px 30px rgba(0, 0, 0, 0.1);
            display: flex;
            flex-direction: column;
            overflow: hidden;
            border: 1px solid #ddd;
        }

        /* Header (College Branding) */
        .chatbot-header {
            /* Using a common college/education color: Green */
            background-color: #4CAF50;
            color: white;
            padding: 15px;
            text-align: center;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
        }

        .chatbot-title {
            margin: 0;
            font-size: 1.1em;
            font-weight: bold;
        }

        /* Chat Body (Message History) */
        .chatbot-body {
            flex-grow: 1;
            padding: 15px;
            overflow-y: auto;
            background-color: #f7f9fb;
            /* Scrollbar styling for better look */
            scrollbar-width: thin;
            scrollbar-color: #ccc transparent;
        }
        .chatbot-body::-webkit-scrollbar {
            width: 8px;
        }
        .chatbot-body::-webkit-scrollbar-thumb {
            background-color: #ccc;
            border-radius: 10px;
        }

        /* Messages */
        .message {
            margin-bottom: 15px;
            max-width: 85%;
            padding: 10px 12px;
            border-radius: 18px;
            line-height: 1.4;
            font-size: 0.9em;
            word-wrap: break-word;
            clear: both; /* Important for alignment */
        }

        .message p {
            margin: 0;
        }

        /* Bot Message Style */
        .bot-message {
            background-color: #e5e5ea; /* Light gray */
            color: #333;
            float: left;
            border-top-left-radius: 5px; /* Subtle variation */
            margin-right: auto;
        }

        /* User Message Style */
        .user-message {
            background-color: #4CAF50; /* Primary/College color */
            color: white;
            float: right;
            border-top-right-radius: 5px; /* Subtle variation */
            margin-left: auto;
        }

        /* Input Area */
        .chatbot-input {
            display: flex;
            padding: 10px;
            border-top: 1px solid #ddd;
            background-color: #fff;
        }

        #user-input {
            flex-grow: 1;
            padding: 10px 15px;
            border: 1px solid #ccc;
            border-radius: 20px;
            margin-right: 8px;
            font-size: 0.9em;
            outline: none;
            transition: border-color 0.3s;
        }
        #user-input:focus {
            border-color: #4CAF50;
        }

        #send-button {
            background-color: #4CAF50;
            color: white;
            border: none;
            padding: 10px 15px;
            border-radius: 20px;
            cursor: pointer;
            font-size: 0.9em;
            font-weight: bold;
            transition: background-color 0.3s, transform 0.1s;
        }

        #send-button:hover {
            background-color: #45a049;
        }
        #send-button:active {
            transform: scale(0.98);
        }
    </style>
</head>
<body>

    <div class="chatbot-container">
        <div class="chatbot-header">
            <h3 class="chatbot-title">Yadav College Chatbot 🎓</h3>
        </div>
        <div class="chatbot-body" id="chat-history">
            <div class="message bot-message">
                <p>Hello! I'm your virtual assistant for Yadhava College. I have information on **History**, **Admission Process**, **Fee Structure**, and **Courses**.</p>
            </div>
            
            <div class="message user-message">
                <p>Tell me about the admission process.</p>
            </div>

            <div class="message bot-message">
                <p>The **Admission Process** is based on academic merit and follows the guidelines of Madurai Kamaraj University. Please refer to the official college notice for the latest dates and eligibility criteria, which are usually published in May/June.</p>
            </div>
            
            <div class="message user-message">
                <p>What are the available undergraduate courses?</p>
            </div>
            
            <div class="message bot-message">
                <p>We offer various UG programs including B.A. History, B.Sc. Physics, B.Com., and more. For the full list and course outcomes, please check the 'Courses Outcome' section on the college website.</p>
            </div>
            
            <div class="message bot-message">
                <p>Is there anything else I can assist you with, such as **Fee Structure** or **Scholarship** details?</p>
            </div>

        </div>
        <div class="chatbot-input">
            <input type="text" id="user-input" placeholder="Type your message here..." />
            <button id="send-button">Send</button>
        </div>
    </div>

</body>
</html>
